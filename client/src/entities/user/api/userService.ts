import type { UserLogin } from "../types/schema";
import { UserResponseSchema, type UserRegister, type UserResponse } from "../types/schema";


export class UserService { 
    static async signUp (formData: UserRegister) {

    }
}